name: Apply final reported content fixes

on:
  push:
    branches: [main]
    paths:
      - 'scripts/apply-final-user-batch-20260814.mjs'
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: aparatchi-content-write-lock
  cancel-in-progress: false

jobs:
  patch-test:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 50
      - uses: actions/setup-node@v6
        with:
          node-version: '22'
      - name: Apply final fixes
        run: node scripts/apply-final-user-batch-20260814.mjs
      - name: Check sync syntax
        run: node --check scripts/sync-upera.mjs
      - name: Run regression suite
        run: node --test scripts/tests/*.test.mjs
      - name: Commit verified source
        shell: bash
        run: |
          set -euo pipefail
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add scripts
          if git diff --cached --quiet; then exit 0; fi
          git commit -m "fix: lock language operator artwork and Persian metadata truth"
          PUSHED=0
          for attempt in 1 2 3 4 5; do
            if git pull --rebase origin main && git push origin HEAD:main; then PUSHED=1; break; fi
            git rebase --abort >/dev/null 2>&1 || true
            sleep $((attempt * 2))
          done
          [ "$PUSHED" -eq 1 ] || exit 1
