name: Apply final stability v2

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/apply-final-stability-v2.yml'
      - 'scripts/apply-final-stability-fixes.mjs'
      - 'final-fix-trigger.txt'
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: aparatchi-content-final-v2
  cancel-in-progress: false

jobs:
  apply:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6
        with:
          fetch-depth: 20

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: '22'

      - name: Apply and test final Content fixes
        shell: bash
        run: |
          set +e
          : > final-v2-output.log
          node scripts/apply-final-stability-fixes.mjs >> final-v2-output.log 2>&1
          patch_code=$?
          if [ "$patch_code" -eq 0 ]; then
            node --test scripts/tests/*.test.mjs >> final-v2-output.log 2>&1
            test_code=$?
          else
            test_code=0
          fi
          cat final-v2-output.log
          if [ "$patch_code" -ne 0 ] || [ "$test_code" -ne 0 ]; then
            code="$patch_code"
            if [ "$code" -eq 0 ]; then code="$test_code"; fi
            git restore scripts/sync-upera.mjs scripts/client-catalog.mjs .github/workflows/sync-upera.yml scripts/tests/sync-upera-regression.test.mjs 2>/dev/null || true
            rm -f scripts/tests/final-stability.test.mjs
            git config user.name "github-actions[bot]"
            git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
            git add final-v2-output.log
            git commit -m "diagnostic: Content v2 final fixer failure" || true
            git pull --rebase --autostash origin main
            git push origin HEAD:main
            exit "$code"
          fi
          rm -f final-v2-output.log

      - name: Commit validated sources
        shell: bash
        run: |
          set -euo pipefail
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          rm -f final-fix-output.log final-v2-output.log
          # sync-upera.yml is configured directly through the GitHub connector.
          # Do not include workflow files in the bot commit because the job token
          # intentionally lacks the workflows permission.
          git restore .github/workflows/sync-upera.yml 2>/dev/null || true
          git add scripts/sync-upera.mjs scripts/client-catalog.mjs scripts/tests/sync-upera-regression.test.mjs scripts/tests/final-stability.test.mjs
          git add -A final-fix-output.log final-v2-output.log 2>/dev/null || true
          if git diff --cached --quiet; then
            echo "No source changes were needed."
            exit 0
          fi
          git commit -m "fix: stabilize series media people and episode backfill"
          git pull --rebase --autostash origin main
          git push origin HEAD:main
